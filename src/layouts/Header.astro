---
import Navigation from '../layouts/Navigation.astro'
import Badges from './Badges.astro';
import Logo from './Logo.astro';

const { links, badges, sotd } = Astro.props;
---
<header>

  <Logo size="200" />
  <Navigation links={ links } sotd={ sotd } />
  <Badges badges={ badges } />
  
</header>